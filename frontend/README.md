# frontend

A new Flutter project.

## Getting Started

To run this project, ensure you have Flutter installed. Then, navigate to the project directory and run:

```bash
flutter pub get
flutter run
```